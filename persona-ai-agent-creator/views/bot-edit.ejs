<%- include("partials/header") %>

<%- include("_bot-form", {
  pageTitle: t('form.editTitle') + ": " + bot.service_name,
  pageSubtitle: t('form.editSubtitle'),
  formAction: "/bots/" + bot.id,
  cancelUrl: "/bots/" + bot.id,
  submitLabel: t('form.saveChanges'),
  isEdit: true,
  bot: bot
}) %>

<%- include("partials/footer") %>
